# nfc-reader-RCS300

Read NFC tags using SONY RC-S300 NFC card reader with Python.

# Install 
```
$ python3 -m pip install pyscard

$ sudo apt install libusb-dev libpcsclite-dev opensc pcscd pcsc-tools

$ pip install python-osc
```


